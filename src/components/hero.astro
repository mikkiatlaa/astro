---

---

<section class="hero">
    <video autoplay muted id="myVideo">
        <source src="../heroVid.mp4" type="video/mp4" />
    </video>
    <div class="hero-text">
        <p>Vi er en frivillig forening som hjælper unge talenter i den danske reklame- og kommunikationsbrnache med at netværke, udvikle dit portfilio og få foden indenfor i den kreative branche.</p>
    </div>
</section>

<style>
    #myVideo {
        object-fit: cover;
        position: absolute;
        width: 100vw;
        height: 70vh;
        z-index: 1;
    }
    .hero {
        display: flex;
        flex-direction: column;
        justify-content: flex-end;
        align-items: center;
        height: 70vh;
        background-color: #a8daff;
        z-index: -1;
    }

    .hero-text {
        max-width: 570px;
        z-index: 2;
        bottom: 0;
        margin-bottom: 5vh;
    }

    .hero p {
        margin-bottom: 0;
    }
</style>
